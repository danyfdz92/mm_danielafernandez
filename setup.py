from setuptools import setup, find_packages

setup(
    name="mm_data_production",
    version="0.1",
    packages=find_packages(),
    include_package_data=True,
    install_requires=[],  # Add dependencies here, e.g., ['pandas', 'numpy']
)